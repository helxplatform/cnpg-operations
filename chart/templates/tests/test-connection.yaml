apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cnpg-operations.fullname" . }}-test-connection"
  labels:
    {{- include "cnpg-operations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cnpg-operations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
